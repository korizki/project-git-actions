name: Auto Deployment Update
on:
  push:
    branches: [ master ]
jobs:
  web-deploy:
    name: 🎉 Auto Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: 📂 Sync files
      uses: burnett01/rsync-deployments@4.1
      with:
        switches: -avzr --delete --exclude=".git" --exclude=".github"
        path: /
        remote_path: public_html/github_auto/project-git-actions
        remote_host: da.jagadhost.com
        remote_user: naizadev
        remote_key: SHA256:qMhZ/AUtGPaaMM6ydRoLSWtOQ2eP4k43bhfi3tbCOKs
